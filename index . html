<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wendy Rose Will You Be My Valentine?</title>
    <style>
        :root {
            --primary-pink: #ff4d6d;
            --soft-pink: #ffb3c1;
            --bg-purple: #9b5de5;
            --white-glass: rgba(255, 255, 255, 0.9);
        }

        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f08080, #c77dff);
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* The Main Dashboard Container */
        .desktop-wrapper {
            width: 90vw;
            height: 85vh;
            background: #f8ad9d; /* Matches the video's pinkish hue */
            border: 8px solid #333;
            border-radius: 30px;
            position: relative;
            box-shadow: 0 20px 50px rgba(0,0,0,0.3);
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        /* Top Bar Decorations */
        .top-decor {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        .calendar {
            background: white;
            padding: 15px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 5px 5px 0px var(--primary-pink);
        }

        .calendar .month { color: #555; font-size: 1.2rem; font-weight: bold; }
        .calendar .day { color: var(--primary-pink); font-size: 2.5rem; font-weight: 900; }

        /* Main Content Area */
        .main-stage {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .scene { display: none; width: 100%; }
        .active { display: block; animation: zoomIn 0.6s ease; }

        h1 { font-size: 3.5rem; color: #fff; text-shadow: 3px 3px 0px var(--primary-pink); margin: 20px 0; }

        /* Buttons */
        .btn-group { display: flex; gap: 20px; justify-content: center; margin-top: 30px; }
        .btn {
            padding: 15px 40px;
            font-size: 1.5rem;
            border: none;
            border-radius: 15px;
            cursor: pointer;
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0 5px 0px #888;
        }
        .yes-btn { background: #4cc9f0; color: white; }
        .no-btn { background: #f72585; color: white; position: relative; }
        .btn:active { transform: translateY(4px); box-shadow: 0 1px 0px #888; }

        /* Gift Section */
        .gift-container {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 20px;
        }
        .gift-card {
            background: white;
            padding: 30px;
            border-radius: 20px;
            width: 150px;
            cursor: pointer;
            transition: transform 0.3s;
            border-bottom: 5px solid #ddd;
        }
        .gift-card:hover { transform: translateY(-10px); background: #fff1f2; }

        /* Animations */
        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.9); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Decorative Floating Icons */
        .heart-float { position: absolute; font-size: 2rem; color: white; opacity: 0.6; }
    </style>
</head>
<body>

    <div class="desktop-wrapper">
        <div class="top-decor">
            <div style="font-size: 4rem;">💌</div>
            <div class="calendar">
                <div class="month">FEB</div>
                <div class="day">14</div>
            </div>
        </div>

        <div class="main-stage">
            <div id="scene1" class="scene active">
                <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExOHp6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsJmVwPXYxX2ludGVybmFsX2dpZl9ieV9pZCZjdD1z/v4aP9DIs8TsnqH1O6E/giphy.gif" height="150">
                <h1>Hey there, <br>Will you be my Valentine?</h1>
                <div class="btn-group">
                    <button class="btn yes-btn" onclick="goToScene(2)">YES OF COURSE</button>
                    <button id="noBtn" class="btn no-btn" onmouseover="dodge()">NO THANK YOU</button>
                </div>
            </div>

            <div id="scene2" class="scene">
                <h1>Yay, you said yes!</h1>
                <p style="color: white; font-size: 1.5rem;">I made all these for you, babe:</p>
                <div class="gift-container">
                    <div class="gift-card" onclick="goToScene(3)"><h3>Gift 1</h3>🎁</div>
                    <div class="gift-card" onclick="goToScene(3)"><h3>Gift 2</h3>🎁</div>
                    <div class="gift-card" onclick="goToScene(3)"><h3>Gift 3</h3>🎁</div>
                </div>
            </div>

            <div id="scene3" class="scene">
                <h1>Our Rose Bouquet</h1>
                <img src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExOHp6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsbmZ6eHpsJmVwPXYxX2ludGVybmFsX2dpZl9ieV9pZCZjdD1z/3o7TKoH687qJ4nQzKw/giphy.gif" height="250">
                <p style="color: white; font-size: 1.2rem; max-width: 500px; margin: 20px auto;">
                    You are the best part of my life. I can't imagine a day without you in it.
                </p>
                <button class="btn yes-btn" onclick="location.reload()">Click to Restart</button>
            </div>
        </div>
    </div>

    <script>
        function goToScene(sceneId) {
            document.querySelectorAll('.scene').forEach(s => s.classList.remove('active'));
            document.getElementById('scene' + sceneId).classList.add('active');
        }

        function dodge() {
            const btn = document.getElementById('noBtn');
            const x = Math.random() * (window.innerWidth - 200) - (window.innerWidth / 2);
            const y = Math.random() * (window.innerHeight - 100) - (window.innerHeight / 2);
            btn.style.position = 'absolute';
            btn.style.left = Math.random() * 70 + '%';
            btn.style.top = Math.random() * 70 + '%';
        }
    </script>
</body>
</html>
